The ServerForm is the Server Form for the lab activity.
The ClientForm is tht form used by the client connections.

The "Testing" and "Lab17-server" are only console based versions to test functionality.

use the "serverForm" and "clientForm"

server is set up with 127.0.0.1:2048
